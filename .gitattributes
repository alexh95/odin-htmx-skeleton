# Normalize to LF in the repo and on checkout, regardless of platform.
* text=auto eol=lf

# Shell scripts must stay LF even on Windows, or the shebang/execution breaks.
*.sh text eol=lf

# Windows batch files want CRLF.
*.bat text eol=crlf
