{
  "name": "odin-htmx-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the Odin + HTMX showcase.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
